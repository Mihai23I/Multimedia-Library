<h1>Log in</h1>
<br>
<i><p>Daca nu ai inca un cont, poti crea unul <%= link_to "aici", sign_up_path %>.</p></i>
<p>Introdu datele pentru a accesa contul de utilizator:</p>
<%= form_for :session, url: login_path do |f| %>
  <div class="form-group row">
    <%= f.label :email, class: "col-sm-2 col-form-label" %>
    <%= f.email_field :email, class: "textinput", placeholder: "Introdu adresa de mail" %>
  </div>

  <div class="form-group row">
    <%= f.label :parola, class: "col-sm-2 col-form-label" %>
    <%= f.password_field :password, class: "textinput" %>
  </div>

  <div class="form-group row">
    <%= f.label :remember, class: "checkbox inline" do %>
      <%= f.check_box :remember %>
      Tine-ma minte
    <% end %>
  </div>

  <%= f.submit "Logheaza-te in cont", class: "btn btn-primary" %>
<% end %>
<br>
<p> In cazul in care ti-ai uitat parola, acceseaza acest <%= link_to "link", new_user_password_reset_path %>.</p>
