<%= form_for([:administrator, @physical_item], html: { class: 'form-horizontal physical_item' }) do |f| %>
  <% if @physical_item.errors.any? %>
    <div id="error_explanation" class="panel panel-danger">
      <div class="panel-heading">
        <h2 class="panel-title"><%= pluralize(@physical_item.errors.count, "error") %> prohibited this physical_item from being saved:</h2>
      </div>
      <div class="panel-body">
        <ul>
        <% @physical_item.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
        </ul>
      </div>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :id, class: 'control-label col-md-2' %>
    <div class="col-md-10">
      <%= f.number_field :id, class: 'form-control' %>
    </div>
  </div>
  <div class="form-group">
    <%= f.label :item_id, class: 'control-label col-md-2' %>
    <div class="col-md-10">
      <%= f.number_field :item_id, class: 'form-control' %>
    </div>
  </div>
  <div class="form-group">
    <%= f.label :location_id, class: 'control-label col-md-2' %>
    <div class="col-md-10">
      <%= f.number_field :location_id, class: 'form-control' %>
    </div>
  </div>
  <div class="form-group">
    <%= f.label :category, class: 'control-label col-md-2' %>
    <div class="col-md-10">
      <%= f.text_field :category, class: 'form-control' %>
    </div>
  </div>
  <div class="form-group">
    <div class="col-md-offset-2 col-md-10">
      <%= f.submit nil, class: 'btn btn-primary' %>
    </div>
  </div>
<% end %>
